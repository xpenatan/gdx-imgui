project.ext {
    groupId = "com.github.xpenatan.jDear-Imgui"
    var isRelease = System.getenv('RELEASE')
    if(isRelease != null && isRelease.toBoolean()) {
        jDearImguiVersion = "1.0.0-b1"
    }
    else {
        jDearImguiVersion = "1.0.0-SNAPSHOT"
    }

    System.out.println("jDear-imgui Version: " + jDearImguiVersion)

    ImLayoutVersion = jDearImguiVersion

    gdxTeaVMVersion = "1.0.0-SNAPSHOT"
    gdxVersion = "1.11.0"
    jniGenVersion = "2.3.1"
    gdxFrameViewportVersion = "1.0.0-SNAPSHOT"
    jUnitVersion = "4.12"
    jParserVersion = "1.0.0-SNAPSHOT"
}