plugins {
    id 'java'
}

import org.gradle.internal.os.OperatingSystem

dependencies {
    implementation project(":examples:imlayout:core")
    implementation "com.badlogicgames.gdx:gdx-backend-lwjgl3:$project.gdxVersion"
    implementation "com.badlogicgames.gdx:gdx-platform:$gdxVersion:natives-desktop"

    // Repo Lib
    implementation "com.github.xpenatan.jDear-Imgui:core-platform:$project.jDearImguiVersion:natives-desktop"
    implementation "com.github.xpenatan.jDear-Imgui:ImLayout-platform:$project.jDearImguiVersion:natives-desktop"

    // Local Lib
//    implementation files("../../../imgui/imgui-core/imgui-core/libs/imgui-core-natives.jar")
//    implementation files("../../../imgui/imgui-extensions/imgui-imlayout/imgui-imlayout/libs/imlayout-natives.jar")
}

project.ext.mainClassName = "com.github.xpenatan.imgui.example.imlayout.Main"
project.ext.assetsDir = new File("../assets");

task runDesktopImLayoutExample(dependsOn: classes, type: JavaExec) {
    setGroup("examples")
    setDescription("Run ImLayout example")
    mainClass.set(project.mainClassName)
    setClasspath(sourceSets.main.runtimeClasspath)
    workingDir = project.assetsDir

    if (OperatingSystem.current() == OperatingSystem.MAC_OS) {
        // Required to run on macOS
        jvmArgs += "-XstartOnFirstThread"
    }
}