plugins {
    id 'java'
}

apply from: '../../dependencies.gradle'

def imgui_version = project.jDearImguiGDXVersion
def imgui_name = "jDear-Imgui-gdx"

dependencies {
	implementation "com.badlogicgames.gdx:gdx:$project.gdxVersion"

	implementation project(':jDear-imgui')

    // Use JUnit test framework
    testImplementation "junit:junit:$project.jUnitVersion"
}

sourceSets.main.java.srcDirs = [ "src/main/java/" ]
sourceSets.main.resources.srcDirs = ["src/main/java/"]

task fromClasses (type: Jar) {
	from (sourceSets.main.output){
	}
}

publishing {
	publications {
		publisha2(MavenPublication) {
			version jDearImguiGDXVersion
			project.version = jDearImguiGDXVersion
			artifactId imgui_name
			artifact fromClasses
		}
	}
}